<div class="spotify-login">
  {#authorized}
    Welcome back, 
      {#userData.display_name}
        {userData.display_name}
      {:else}
        {userData.id}
      {/userData.display_name}
    !
    <div class="spotify-user-image"><img src="{userData.images[0].url}"/></div>
  {:else}
    <a href="{authUrl}">Click here to log in with Spotify</a>
  {/authorized}
</div>